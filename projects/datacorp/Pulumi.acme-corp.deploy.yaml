settings:
  sourceContext:
    git:
      repoUrl: https://github.com/perlovsky-pulumi/datacorp.git
      branch: refs/heads/main
      repoDir: projects/datacorp
  operationContext:
    options:
      skipInstallDependencies: true
    preRunCommands:
      - cd ../../components/pulumi-secure-bucket && npm install && npm run build
      - npm install
